To find reverse of a number
#include <stdio.h>
int main()
{
    int rem,rev=0,num;
    printf("Enter a number");
    scanf("%d",&num);
    while(num>0)
    {
       rem=num%10;
       num=num/10;
       rev=rev*10+rem;
    }
    printf("Reverse of a number is %d",rev);
     return 0;
}
